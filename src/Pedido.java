/* import java.util.ArrayList;

public class Pedido extends Item {

    public Pedido() {
        super();
    }

    void add_item(int pd, ArrayList<Item> pedido, ArrayList<Item> menuList) {
        pedido.add(menuList.get(pd - 1));
        System.out.println("Item " + pd + " adicionado à lista de pedidos.\n");
    }

    void remov_item(int i, ArrayList<Item> pedido) {
        pedido.remove(i - 1);
    }

    void exibir_pedido(ArrayList<Item> pedido) {
        for (int i = 0; i < pedido.size(); i++) {
            System.out.println((i + 1) + " - " + pedido.get(i).getName());
        }
    }
} */